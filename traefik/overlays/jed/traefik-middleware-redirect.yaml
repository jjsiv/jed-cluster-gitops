apiVersion: traefik.containo.us/v1alpha1
kind: Middleware
metadata:
  name: middleware-traefik-redirect-https
  namespace: traefik
spec:
  redirectScheme:
    scheme: https
    permanent: true
    port: "443"
